  - name: Install ZSH
    apt:
       name: [zsh]

  - name: Backup existing ~/.zshrc
    shell: if [ -f ~/.zshrc ]; then mv ~/.zshrc{,.orig}; fi

  - name: Install OhMyZsh
    shell: wget https://raw.githubusercontent.com/robbyrussell/oh-my-zsh/master/tools/install.sh -0 - | zsh

  - name: Clone new ~/.zshrc
    git:
      repo=https://github.com/fj9/autobuntu/.zshrc
      dest=~/.zshrc
